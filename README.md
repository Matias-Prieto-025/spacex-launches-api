## Para ejecutar:

- npm install
- levantar container con postgres: docker-compose up
- ejecutar migraciones para crear tabla en BD: npx sequelize db:migrate 
- npm start

Corre sobre el puerto 3001 (seteado en .env file)